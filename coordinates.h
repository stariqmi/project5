#ifndef __COORD_H__
#define __COORD_H__

// Class only for storing coordinates, as a helper class.
class Coordinates {
	public:
		int x;
		int y;
		Coordinates(int x, int y);
		~Coordinates();
};

#endif